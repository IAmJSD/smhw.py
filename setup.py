from setuptools import setup

setup(name='smhw.py',
      version='1.0',
      description='A client for the Show My Homework API.',
      license='GNU',
      packages=['smhw'],
      zip_safe=False)
